{
  "permissions": {
    "allow": ["Bash(npm install *)", "Bash(npm run format)", "Bash(npm run format:check)"]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && echo \"$f\" | grep -qE '\\.(ts|tsx|js|jsx|mjs|cjs)$' && npx prettier --write \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
